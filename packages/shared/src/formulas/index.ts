// Pure formula functions shared by server (authoritative) and client (prediction).
// e.g. xpToNext, yieldMult, pRare, effective-stat and duel math (SPEC §8).
// Each takes its constants from the config snapshot as parameters — no hardcoded
// numbers here. Populated from Phase 4 onward. Empty in Phase 0.
export {};
